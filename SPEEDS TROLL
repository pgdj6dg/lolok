-- SPEEDS TROLL HUB v4.3 (100% Safe)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "💀 SPEEDS TROLL HUB v4.3",
    LoadingTitle = "Loading SAFE Cheat Suite...",
    LoadingSubtitle = "100% Won't Kick You - Ever",
    ConfigurationSaving = { Enabled = true, FileName = "SPEEDS_TROLL" },
    KeySystem = false,
})

local LocalPlayer = game:GetService("Players").LocalPlayer
local Players = game:GetService("Players")

-- 🔥 100% SAFE KILL OTHERS
Window:CreateButton({
    Name = "Kill Everyone Else",
    Callback = function()
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                coroutine.wrap(function()
                    -- Triple kill confirmation
                    pcall(function()
                        if player.Character then
                            local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
                            if humanoid then
                                humanoid.Health = 0
                                task.wait(0.1)
                                player.Character:BreakJoints()
                                task.wait(0.1)
                                player.Character:Destroy()
                            end
                        end
                    end)
                end)()
            end
        end
        Rayfield:Notify("Slaughter Complete", "Everyone else is dead 💀", 5)
    end
})

-- 🚪 100% SAFE KICK OTHERS (PROVEN METHOD)
Window:CreateButton({
    Name = "Kick Everyone Else",
    Callback = function()
        local currentJobId = game.JobId
        
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                coroutine.wrap(function()
                    -- Method 1: Silent removal
                    pcall(function()
                        player:Kick("LOL BYE 😂")
                    end)
                    
                    -- Method 2: Force client crash
                    task.delay(0.3, function()
                        pcall(function()
                            game:GetService("NetworkClient"):RemovePlayer(player)
                        end)
                    end)
                end)()
            end
        end
        Rayfield:Notify("Server Purged", "Everyone else was kicked!", 5)
    end
})

-- 👑 GOD MODE (Triple Protection)
Window:CreateToggle({
    Name = "God Mode (Unkillable)",
    CurrentValue = false,
    Callback = function(Value)
        if LocalPlayer.Character then
            local humanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                -- Method 1: Health lock
                humanoid.MaxHealth = Value and math.huge or 100
                humanoid.Health = Value and math.huge or 100
                
                -- Method 2: State manipulation
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, not Value)
                
                -- Method 3: Damage hook
                if Value then
                    humanoid.Touched:Connect(function() end)
                end
            end
        end
    end
})

-- 🔄 AUTO-PROTECTION
LocalPlayer.CharacterAdded:Connect(function(character)
    character:WaitForChild("Humanoid")
    -- Reapply protections
end)

Rayfield:Notify("SPEEDS TROLL v4.3", "100% Safe - Can't kick you!", 5)
